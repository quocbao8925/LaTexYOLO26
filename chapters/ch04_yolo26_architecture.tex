% ============================================================
% CHƯƠNG 4: KIẾN TRÚC YOLO26
% ============================================================
\chapter{Kiến trúc mô hình YOLO26}
\label{ch:architecture}

Chương này trình bày chi tiết kiến trúc mô hình \yolo, bao gồm ba phần chính: Backbone (trích xuất đặc trưng), Neck (tổng hợp đặc trưng đa tỷ lệ), và Detection Head (dự đoán bounding box và lớp đối tượng). Nội dung được tổng hợp từ paper gốc~\cite{yolo26}, mã nguồn Ultralytics~\cite{ultralytics}, và quá trình nghiên cứu xây dựng lại mô hình từ đầu (from scratch) của nhóm.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{YOLO26.drawio.png}
    \caption{Sơ đồ khối tổng quát kiến trúc YOLO26. Đầu vào ảnh $3 \times 640 \times 640$ được xử lý qua Backbone (trích xuất đặc trưng), Neck (tổng hợp FPN+PAN), và Detection Head (Dual-Head O2M/O2O).}
    \label{fig:yolo26_overview}
\end{figure}

% ===================================================================
\section{Backbone}
\label{sec:arch_backbone}

Backbone của \yolo gồm 11 lớp (b0--b10) có nhiệm vụ trích xuất đặc trưng ảnh ở nhiều mức phân giải khác nhau.

\subsection{Khối Conv cơ bản}

Trong \yolo, mỗi khối \texttt{Conv} bao gồm ba phần: lớp tích chập (\texttt{Conv2d}), chuẩn hóa batch (\texttt{BatchNorm2d}), và hàm kích hoạt \texttt{SiLU}:

\begin{lstlisting}[caption={Khối Conv cơ bản trong YOLO26}, label=lst:conv]
class Conv(nn.Module):
    def __init__(self, c1, c2, k=1, s=1, p=None, g=1):
        self.conv = nn.Conv2d(c1, c2, k, s, autopad(k, p), groups=g, bias=False)
        self.bn = nn.BatchNorm2d(c2)
        self.act = nn.SiLU(inplace=True)
    
    def forward(self, x):
        return self.act(self.bn(self.conv(x)))
\end{lstlisting}

Các lớp Conv đầu tiên (b0, b1, b3, b5, b7) sử dụng kernel $3 \times 3$ với stride $s=2$ để giảm kích thước không gian xuống một nửa ở mỗi bước.

\textbf{Conv 1x1} trộn channel tại cùng vị trí spatial. \textbf{Conv 3x3} trộn thông tin không gian (cạnh, biên, texture).

\subsection{Khối DWConv (Depthwise Convolution)}
\label{subsec:dwconv}

DWConv là tích chập tách biệt theo chiều sâu (Depthwise Convolution), trong đó số lượng nhóm tính toán bằng số kênh vào và số kênh ra ($\text{groups} = \gcd(c_1, c_2)$):
\begin{equation}
    \text{Params}_{\text{DW}} = c_1 \times k^2 \quad \ll \quad \text{Params}_{\text{std}} = c_1 \times c_2 \times k^2
\end{equation}
DWConv được sử dụng rộng rãi trong classification head (cv3) của YOLO26 nhằm giảm chi phí tính toán và tăng tốc độ suy luận.

\subsection{Khối Bottleneck}
\label{subsec:bottleneck}

Bottleneck là block residual cơ bản, tạo đặc trưng $F(x)$ bằng 2 lớp Conv rồi tùy điều kiện cộng residual với đầu vào $x$:

\begin{figure}[H]
\centering
\begin{tikzpicture}[node distance=1cm and 1.2cm, auto]
    % Nodes
    \node (input) [splitnode] {};
    \node (lbl_in) [left=0.15cm of input] {Input $x$};
    \node (cv1) [block, right=of input] {Conv 1x1\\(cv1)};
    \node (cv2) [block, right=of cv1] {Conv 3x3 / DWConv\\(cv2)};
    \node (sum) [sum, right=of cv2];
    \node (output) [right=0.8cm of sum] {Output};

    % Connections
    \path [line] (input) -- (cv1);
    \path [line] (cv1) -- (cv2);
    \path [line] (cv2) -- (sum);
    \path [line] (sum) -- (output);
    
    % Skip connection
    \path [line] (input) -- ++(0,1.0) -| (sum.north);
    
    % Labels
    \node [above=0.6cm of cv1, font=\small\itshape] {Skip Connection (Shortcut = True)};
\end{tikzpicture}
\caption{Sơ đồ khối Bottleneck với skip connection.}
\label{fig:bottleneck}
\end{figure}

\begin{equation}
    F(x) = \text{cv2}(\text{cv1}(x))
    \label{eq:bottleneck_Fx}
\end{equation}
\begin{equation}
    \text{Output} = \begin{cases}
        x + F(x) & \text{nếu } \texttt{shortcut} = \texttt{True} \text{ và } c_1 = c_2 \\
        F(x) & \text{ngược lại}
    \end{cases}
    \label{eq:bottleneck_output}
\end{equation}

\noindent trong đó:
\begin{itemize}
    \item \texttt{cv1}: Conv$(c_1, c\_, k_1, s=1)$ với $c\_ = \lfloor c_2 \times e \rfloor$ (kênh ẩn, $e$ là expansion ratio).
    \item \texttt{cv2}: Conv$(c\_, c_2, k_2, s=1)$ với $\texttt{groups}=g$.
    \item Nhánh skip chỉ kích hoạt khi $c_1 = c_2$ và \texttt{shortcut=True}.
\end{itemize}

\textbf{Tác dụng}: Giảm chi phí tính toán (nhờ expansion ratio $e < 1$), cho phép xây dựng mạng sâu hơn mà vẫn nhẹ, và giữ thông tin gốc nhờ nhánh residual giúp tránh vanishing gradient.

Ví dụ: input $[B, 128, 80, 80]$, $e=0.5$ $\to$ $c\_=64$ $\to$ output $[B, 128, 80, 80]$ (chiều dài và chiều rộng giữ nguyên).

\subsection{Khối C2f (Cross Stage Partial with 2 Convolutions)}
\label{subsec:c2f}

Khối C2f~\cite{cspnet} kết hợp tư tưởng của CSPNet nhằm gia tăng số lượng đường truyền gradient, giúp mạng học được nhiều đặc trưng đa dạng hơn:

\begin{equation}
    \text{C2f}(x) = \text{cv2}\Big(\text{Concat}\big[y_0, y_1, m_1(y_1), m_2(m_1(y_1)), \ldots\big]\Big)
\end{equation}

Đầu vào sau khi qua \texttt{cv1} được chia đôi thành 2 nhánh: $[y_0, y_1] = \text{chunk}(\text{cv1}(x), 2)$. Nhánh nông $y_0$ đi thẳng không xử lý sâu thêm, trong khi nhánh $y_1$ được truyền qua một chuỗi các khối Bottleneck $m_i$.

\textbf{Sơ đồ luồng dữ liệu (với n=2)}:
\begin{verbatim}
Input -> cv1 -> split -> [y0, y1]
                              |
                         Bottleneck(y1) -> y2
                              |
                         Bottleneck(y2) -> y3
                              |
              cat(y0, y1, y2, y3) -> cv2 -> Output
\end{verbatim}

\textbf{Ý nghĩa}: Cơ chế split-and-concat này vừa giữ lại các đặc trưng nông thô ($y_0$) vừa trích xuất các đặc trưng ngữ cảnh sâu ($y_3$), giúp gradient lan truyền hiệu quả hơn trong quá trình backward.

\subsection{Khối C3 và C3k}
\label{subsec:c3_c3k}

\textbf{C3} là khối CSP kiểu cũ có hai nhánh chính: nhánh xử lý sâu qua chuỗi Bottleneck và nhánh đi thẳng qua một khối Conv, sau đó ghép lại bằng phép concat và xử lý qua Conv 1x1 cuối.

\textbf{C3k} kế thừa C3 nhưng sử dụng Bottleneck với kernel $k \times k$ tùy chỉnh (thường $k=3$):
\begin{equation}
    \text{C3k}: \text{Bottleneck}_{\text{C3}} \text{ dùng } k=((1,1),(3,3)) \quad \to \quad \text{C3k dùng } k=(3,3)
\end{equation}

\begin{figure}[H]
\centering
\begin{tikzpicture}[node distance=0.8cm and 1.2cm, auto]
    % Nodes
    \node (input) [splitnode] {};
    \node (lbl_in) [left=0.15cm of input] {Input $x$};
    
    % Upper branch (Main branch with Bottlenecks)
    \node (cv1) [block, above right=0.2cm and 1.0cm of input] {Conv 1x1\\(cv1)};
    \node (bottlenecks) [block, right=of cv1] {Chuỗi $n$ khối\\C3k / Bottleneck};
    
    % Lower branch (Shortcut / Skip branch)
    \node (cv2) [block, below right=0.2cm and 1.0cm of input] {Conv 1x1\\(cv2)};
    
    % Concat node
    \node (concat) [grayblock, right=1.2cm of bottlenecks, yshift=-0.7cm] {Concat};
    
    % Final Conv
    \node (cv3) [block, right=of concat] {Conv 1x1\\(cv3)};
    \node (output) [right=0.8cm of cv3] {Output};

    % Connections
    \path [line] (input) -- ++(0,0.7) |- (cv1.west);
    \path [line] (input) -- ++(0,-0.7) |- (cv2.west);
    \path [line] (cv1) -- (bottlenecks);
    \path [line] (bottlenecks.east) -- ++(0.4,0) |- (concat.west);
    \path [line] (cv2.east) -- ++(0.4,0) |- (concat.west);
    \path [line] (concat) -- (cv3);
    \path [line] (cv3) -- (output);
\end{tikzpicture}
\caption{Sơ đồ khối C3k --- C3 với kernel 3x3 sâu hơn.}
\label{fig:c3k}
\end{figure}

Khối C3k có khả năng bao quát thông tin không gian tốt hơn nhờ mở rộng kernel tích chập ở nhánh sâu, giúp học được các đặc trưng biên và cấu trúc hình học tốt hơn, đổi lại chi phí tính toán cao hơn Bottleneck thông thường.

\subsection{Khối C3k2}
\label{subsec:c3k2}

C3k2 là khối trộn đặc trưng chính và quan trọng nhất trong \yolo, kế thừa từ C2f nhưng cho phép tùy biến loại block bên trong:

\begin{equation}
    y_0, y_1 = \text{Split}(\text{cv1}(x))
    \label{eq:c3k2_split}
\end{equation}
\begin{equation}
    z_i = \text{Block}_i(z_{i-1}), \quad z_0 = y_1, \quad i = 1, \ldots, n
    \label{eq:c3k2_blocks}
\end{equation}
\begin{equation}
    \text{Output} = \text{cv2}\big(\text{cat}([y_0, y_1, z_1, \ldots, z_n])\big)
    \label{eq:c3k2_output}
\end{equation}

\begin{figure}[H]
\centering
\begin{tikzpicture}[
    node distance=0cm,
    block/.append style={minimum width=1.6cm, minimum height=0.8cm},
    auto
]
    % --- Node definitions ---
    \node (input)  []                            {Input $x$};
    \node (cv1)    [block, right=1.0cm of input] {Conv 1x1\\(cv1)};
    % Place Concat with explicit gap so shortcut wire has room above
    \node (concat) [grayblock, right=5.5cm of cv1] {Concat};
    \node (cv2)    [block,  right=0.8cm of concat] {Conv 1x1\\(cv2)};
    \node (output) [right=0.8cm of cv2]            {Output};

    % Split point on the wire after cv1
    \coordinate (S) at ($(cv1.east)+(0.55,0)$);

    % Block row, centred below the cv1-to-concat gap
    \coordinate (midgap) at ($(cv1.east)!0.5!(concat.west)$);
    % Lower the block row to y = -1.5cm so we have a clear vertical entry into Concat
    \node (block1) [block, below=1.5cm of midgap, xshift=-1.0cm] {Block 1};
    \node (block2) [block, right=0.7cm of block1]               {Block 2};

    % --- Forward main wire ---
    \path [line] (input) -- (cv1);
    \draw [-]    (cv1.east) -- (S);   % no arrowhead at split

    % --- Shortcut y0: go UP by 1.0cm, then RIGHT to concat.north ---
    % Path-based node placement to ensure label is perfectly centered and above the line
    \coordinate (Sup) at ($(S)+(0,1.0)$);
    \path [line] (S) -- (Sup) -- node[above, midway, font=\small] {Nhánh $y_0$ (đi thẳng)} (Sup -| concat.north) -- (concat.north);

    % --- Branch y1: go DOWN then RIGHT to block1.west ---
    % Single bend: goes down to block1's center line, then horizontally to block1.west
    \path [line] (S) |- (block1.west);
    \node [left=5pt, font=\small] at (S |- block1.west) {$y_1$};

    % --- Block 1 to Block 2 ---
    \path [line] (block1) -- (block2);

    % --- Outputs from blocks to concat.south ---
    % Block 1 output (from mid12) goes up to y = -0.9, then right to concat.south-west, then up
    \coordinate (mid12)  at ($(block1.east)!0.5!(block2.west)$);
    \path [line] (mid12) -- ($(mid12)+(0,0.6)$) -| ($(concat.south)+(-0.35,0)$);

    % Block 2 output (from block2.east) goes horizontally right to concat.south-east's x, then straight up
    \path [line] (block2.east) -| ($(concat.south)+(+0.35,0)$);

    % --- Concat onwards ---
    \path [line] (concat) -- (cv2);
    \path [line] (cv2)    -- (output);
\end{tikzpicture}
\caption{Sơ đồ khối C3k2 --- forward theo C2f, block bên trong tùy cấu hình.}
\label{fig:c3k2}
\end{figure}

Tùy vào 2 tham số boolean \texttt{attn} và \texttt{c3k}, các Block bên trong sẽ được khởi tạo khác nhau:
\begin{enumerate}
    \item \texttt{attn=True}: Block = Bottleneck + PSABlock (Position-Sensitive Attention).
    \item \texttt{attn=False, c3k=True}: Block = C3k (kernel $k \times k$ sâu).
    \item \texttt{attn=False, c3k=False}: Block = Bottleneck tiêu chuẩn.
\end{enumerate}

\begin{table}[H]
\centering
\small
\caption{Sử dụng C3k2 tại các vị trí trong YOLO26.}
\label{tab:c3k2_usage}
\begin{tabular}{lcccl}
\toprule
\textbf{Layer} & \textbf{c3k} & \textbf{attn} & \textbf{Module bên trong} & \textbf{Vị trí} \\
\midrule
b2, b4 & False & False & Bottleneck đơn giản & Backbone đầu \\
b6, b8 & True & False & C3k (3x3 sâu) & Backbone P4, P5 \\
h13--h19 & True & False & C3k & Neck \\
h22 & True & True & Bottleneck + PSABlock & Neck P5 output \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Ví dụ sự thay đổi kích thước tensor}: C3k2($c_1=1024, c_2=256, n=2, \text{c3k}=\text{True}, e=0.5$):
\begin{verbatim}
[B, 1024, 80, 80] -> cv1 -> [B, 256, 80, 80]
    -> split: y0 [B, 128, 80, 80], y1 [B, 128, 80, 80]
    -> C3k(y1) -> y2 [B, 128, 80, 80]
    -> C3k(y2) -> y3 [B, 128, 80, 80]
    -> cat(y0,y1,y2,y3) -> [B, 512, 80, 80]
    -> cv2 -> [B, 256, 80, 80]
\end{verbatim}
Kích thước không gian $H, W$ được giữ nguyên hoàn toàn do tất cả các lớp tích chập bên trong đều sử dụng stride=1 và cơ chế tự động đệm (autopad).

\subsection{Khối SPPF (Spatial Pyramid Pooling Fast)}
\label{subsec:sppf}

SPPF mở rộng vùng nhận cảm ngữ cảnh (receptive field) bằng cách áp dụng liên tiếp 3 lớp MaxPool $5 \times 5$ song song với việc lưu lại kết quả trung gian, mà không làm giảm kích thước không gian ảnh đầu vào:

\begin{figure}[H]
\centering
\begin{tikzpicture}[node distance=0cm, auto]
    % --- Top row: processing chain ---
    \node (input) []                             {Input $x$};
    \node (cv1)   [block,       right=0.8cm of input] {Conv 1x1\\(cv1)};
    \node (mp1)   [orangeblock, right=1.5cm of cv1]   {MaxPool 5x5\\(mp1)};
    \node (mp2)   [orangeblock, right=1.5cm of mp1]   {MaxPool 5x5\\(mp2)};
    \node (mp3)   [orangeblock, right=1.5cm of mp2]   {MaxPool 5x5\\(mp3)};

    % --- Bottom row: Concat well below so drop-lines have clearance ---
    \node (concat) [grayblock, below=2.2cm of mp2]     {Concat};
    \node (cv2)    [block,     right=1.0cm of concat]  {Conv 1x1\\(cv2)};
    \node (output) [right=0.8cm of cv2]                {Output};

    % --- Tap coordinates: midpoints of each gap ---
    \coordinate (T0) at ($(cv1.east)!0.5!(mp1.west)$);
    \coordinate (T1) at ($(mp1.east)!0.5!(mp2.west)$);
    \coordinate (T2) at ($(mp2.east)!0.5!(mp3.west)$);
    \coordinate (T3) at ($(mp3.east)+(0.6,0)$);

    % --- Main chain connections ---
    \path [line] (input) -- (cv1);
    \draw [-]    (cv1.east) -- (T0);
    \path [line] (T0)       -- (mp1.west);
    \draw [-]    (mp1.east) -- (T1);
    \path [line] (T1)       -- (mp2.west);
    \draw [-]    (mp2.east) -- (T2);
    \path [line] (T2)       -- (mp3.west);
    \draw [-]    (mp3.east) -- (T3);

    % --- Vertical drops with two separate horizontal levels to prevent overlaps ---
    \coordinate (L_high) at ($(concat.north)+(0,1.2)$);
    \coordinate (L_low)  at ($(concat.north)+(0,0.6)$);

    \coordinate (Concat1) at ($(concat.north)+(-0.6,0)$);
    \coordinate (Concat2) at ($(concat.north)+(-0.2,0)$);
    \coordinate (Concat3) at ($(concat.north)+(+0.2,0)$);
    \coordinate (Concat4) at ($(concat.north)+(+0.6,0)$);

    \path [line] (T0) -- (T0 |- L_high) -| (Concat1);
    \path [line] (T1) -- (T1 |- L_low)  -| (Concat2);
    \path [line] (T2) -- (T2 |- L_low)  -| (Concat3);
    \path [line] (T3) -- (T3 |- L_high) -| (Concat4);

    % --- Concat to output ---
    \path [line] (concat) -- (cv2);
    \path [line] (cv2)    -- (output);
\end{tikzpicture}
\caption{Sơ đồ khối SPPF --- MaxPool liên tiếp tạo multi-scale receptive field.}
\label{fig:sppf}
\end{figure}

\begin{equation}
    y_0 = \text{cv1}(x), \quad
    y_1 = \text{MaxPool}(y_0), \quad
    y_2 = \text{MaxPool}(y_1), \quad
    y_3 = \text{MaxPool}(y_2)
    \label{eq:sppf}
\end{equation}
\begin{equation}
    \text{Output} = \text{cv2}\big(\text{cat}([y_0, y_1, y_2, y_3])\big)
\end{equation}

\begin{figure}[H]
\centering
\begin{tikzpicture}[scale=0.35]
    % Draw grids/boxes
    % Outer box 13x13 (represented as coordinates from -6.5 to 6.5)
    \draw[thick, draw=red!70!black, fill=red!5, fill opacity=0.4] (-6.5,-6.5) rectangle (6.5,6.5);
    % Middle box 9x9 (from -4.5 to 4.5)
    \draw[thick, draw=orange!80!black, fill=orange!5, fill opacity=0.5] (-4.5,-4.5) rectangle (4.5,4.5);
    % Inner box 5x5 (from -2.5 to 2.5)
    \draw[thick, draw=blue!80!black, fill=blue!5, fill opacity=0.6] (-2.5,-2.5) rectangle (2.5,2.5);
    
    % Draw center pixel
    \draw[fill=black] (-0.5,-0.5) rectangle (0.5,0.5);
    \node[white, font=\tiny] at (0,0) {C};

    % Draw grid lines for illustration
    \draw[step=1cm, gray!30, very thin] (-6.5,-6.5) grid (6.5,6.5);

    % Labels
    \node[blue!80!black, font=\small\bfseries] at (0, 1.2) {$5 \times 5$};
    \node[orange!80!black, font=\small\bfseries] at (0, 3.2) {$9 \times 9$};
    \node[red!70!black, font=\small\bfseries] at (0, 5.2) {$13 \times 13$};
    
    % Legends on the right
    \node[anchor=west, font=\small] at (7.5, 3) {\textcolor{blue!80!black}{\textbf{Lần 1 (mp1)}}: Receptive Field $5 \times 5$};
    \node[anchor=west, font=\small] at (7.5, 1) {\textcolor{orange!80!black}{\textbf{Lần 2 (mp2)}}: Receptive Field $9 \times 9$};
    \node[anchor=west, font=\small] at (7.5, -1) {\textcolor{red!70!black}{\textbf{Lần 3 (mp3)}}: Receptive Field $13 \times 13$};
    \node[anchor=west, font=\small] at (7.5, -3) {\textbf{C}: Điểm ảnh trung tâm (Center Pixel)};
\end{tikzpicture}
\caption{Minh họa sự mở rộng receptive field tương đương $5\times5$, $9\times9$, và $13\times13$ qua 3 lần MaxPool 5x5 liên tiếp.}
\label{fig:sppf_maxpool}
\end{figure}

Mỗi lần MaxPool $5 \times 5$ (với stride $s=1$, padding $p=2$) mở rộng vùng ngữ cảnh thêm 4 pixel. Qua 3 lần lặp, thông tin ngữ cảnh toàn cục được tổng hợp tương đương một cửa sổ MaxPool có kích thước $13 \times 13$, hỗ trợ mô hình nhận diện vật thể ở nhiều kích thước khác nhau.

\textbf{So với SPP}: SPP dùng 3 kernel song song ($5\times5$, $9\times9$, $13\times13$); trong khi SPPF dùng cùng một kernel $5\times5$ nối tiếp --- cho receptive field tương đương nhưng \textbf{giảm đáng kể chi phí tính toán}.

\begin{notebox}[Lưu ý: YOLO26 SPPF có act=False và shortcut=True]
Khác với SPPF gốc, YOLO26 SPPF sử dụng \texttt{act=False} trên cv1 (không dùng SiLU), giúp giảm biểu diễn phi tuyến ngay trước bước max-pooling và ổn định thông tin biên. Ngoài ra, YOLO26 SPPF bật \texttt{shortcut=True} tạo kết nối residual ($y + x$) khi $c_1 = c_2$, giúp bảo toàn đặc trưng gốc của tầng P5 qua lớp pooling.
\end{notebox}

\subsection{Cơ chế Attention (Multi-Head Self-Attention)}
\label{subsec:attention}

Trong các cấu hình nâng cao của YOLO26, Multi-Head Self-Attention được chèn vào Neck (h22) và Backbone (b10) để bắt cặp các tương quan không gian toàn cục:
\begin{figure}[H]
\centering
\begin{tikzpicture}[node distance=0cm, auto,
    every node/.style={font=\small},
    block/.append style={minimum width=1.6cm},
    purpleblock/.append style={minimum width=1.4cm}
]

    % Main pipeline nodes
    \node (input)   []                              {Input $x$};
    \node (qkv)     [block, right=0.6cm of input]   {QKV Proj.\\(Conv 1x1)};

    % Q, K, V stacked vertically
    \node (k)       [purpleblock, right=0.8cm of qkv]               {Key $K$};
    \node (q)       [purpleblock, above=0.2cm of k]                 {Query $Q$};
    \node (v)       [purpleblock, below=0.2cm of k]                 {Value $V$};

    % Softmax block
    \node (softmax) [grayblock, right=0.8cm of k, yshift=0.5cm, minimum width=2.2cm, font=\footnotesize]
                    {Softmax\\$\left(\dfrac{Q{\cdot}K^T}{\sqrt{d_k}}\right)$};

    % Attn*V block on the main line
    \node (matmul)  [grayblock, right=3.8cm of k]                   {Attn$\,{\cdot}\,V$};

    % PE node below matmul
    \node (pe)      [block, below=0.6cm of matmul]                  {PE (DWConv 3x3)};

    % Sum nodes on the main line
    \node (sum_pe)  [sum, right=0.8cm of matmul];
    \node (sum_res) [sum, right=0.8cm of sum_pe];
    \node (output)  [right=0.6cm of sum_res]                        {Output};

    % --- connections ---
    % Input to QKV
    \path [line] (input) -- (qkv);

    % QKV fan-out to Q, K, V
    \path [line] (qkv.east) -- ++(0.4,0) |- (q.west);
    \path [line] (qkv.east) -- ++(0.4,0) |- (k.west);
    \path [line] (qkv.east) -- ++(0.4,0) |- (v.west);

    % Q and K to Softmax (entering separate vertical offsets horizontally, no overlaps)
    % Q.east enters Softmax at y = +0.25 (relative to softmax center)
    % K.east enters Softmax at y = -0.25 (relative to softmax center)
    \coordinate (Q_in) at ($(softmax.west)+(0, 0.25)$);
    \coordinate (K_in) at ($(softmax.west)+(0,-0.25)$);
    
    \path [line] (q.east) -- ++(0.4,0) |- (Q_in);
    \path [line] (k.east) -- ++(0.4,0) |- (K_in);

    % Softmax to Attn*V
    \path [line] (softmax.east) -- ++(0.4,0) |- (matmul.west);

    % V to Attn*V (south port) and PE (west port)
    % Single horizontal segment from V east, splitting at vbranch
    \coordinate (vbranch) at ($(v.east)+(0.4,0)$);
    \coordinate (v_up)    at ($(vbranch)+(0,0.35)$);
    \draw [-] (v.east) -- (vbranch);
    
    % Branch to PE: down then horizontally into PE west
    \path [line] (vbranch) |- (pe.west);
    
    % Branch to Attn*V: go up to clear PE, then horizontally right, then straight up into matmul south
    \path [line] (vbranch) -- (v_up) -- (v_up -| matmul.south) -- (matmul.south);

    % Attn*V and PE converge at sum_pe (entering west/south ports)
    \path [line] (matmul.east) -- (sum_pe.west);
    \path [line] (pe.east)     -| (sum_pe.south);

    % sum_pe to sum_res to output
    \path [line] (sum_pe)  -- (sum_res);
    \path [line] (sum_res) -- (output);

    % Global residual connection (centered label on horizontal wire)
    \coordinate (Rup) at ($(input.north)+(0, 1.9)$);
    \path [line] (input.north) -- (Rup) -- node[above, midway, font=\small\itshape] {Global Residual Connection} (Rup -| sum_res.north) -- (sum_res.north);
\end{tikzpicture}
\caption{Sơ đồ khối Attention với QKV projection và Position Encoding.}
\label{fig:attention}
\end{figure}

\begin{align}
    Q, K, V &= \text{split}(\text{QKV\_Conv}(x)) \\
    \text{Attn} &= \text{softmax}\left(\frac{Q \cdot K^T}{\sqrt{d_k}}\right) \\
    \text{Output} &= \text{Proj}\big(V \cdot \text{Attn}^T + \text{PE}(V)\big)
\end{align}

\noindent trong đó $\text{Proj}$ là lớp Conv $1 \times 1$ (không activation) chiếu kết quả attention trở lại không gian kênh ban đầu.

\begin{itemize}
    \item \textbf{QKV Conv}: Lớp tích chập Conv $1\times1$ chiếu đồng thời vector đầu vào sang 3 không gian Query ($Q$), Key ($K$), Value ($V$). Lớp này không sử dụng activation.
    \item $d_k = \texttt{head\_dim} \times \texttt{attn\_ratio}$: Kích thước vector Key được nén nhỏ để tiết kiệm bộ nhớ.
    \item \textbf{PE (Position Encoding)}: Sử dụng một khối Depthwise Conv $3\times3$ tác động lên Value ($V$) nhằm bảo toàn thông tin cấu trúc không gian hình học vốn bị mất trong cơ chế tính toán ma trận Attention thông thường.
    \item \textbf{Multi-head}: Kênh được chia thành $h$ heads riêng biệt để xử lý song song các vùng đặc trưng độc lập.
\end{itemize}

\begin{figure}[H]
\centering
\begin{tikzpicture}[node distance=1.0cm and 1.2cm, auto]
    % Nodes
    \node (x) [grayblock] {Input $x$\\$[B, C, H, W]$};
    \node (qkv) [block, right=of x] {QKV Projection\\(Conv 1x1)};
    \node (split) [purpleblock, right=of qkv] {Query $Q$, Key $K$, Value $V$\\$[B, h, N, d]$};
    \node (attn) [purpleblock, right=of split] {Attention Matrix $A$\\$[B, h, N, N]$};
    \node (out_v) [purpleblock, below=of attn, yshift=0.3cm] {Output Attention\\$[B, h, N, d]$};
    \node (reshape) [block, left=of out_v] {Reshape \& Projection};
    \node (out) [grayblock, left=of reshape] {Output tensor\\$[B, C, H, W]$};

    % Connections
    \path [line] (x) -- (qkv);
    \path [line] (qkv) -- (split);
    \path [line] (split) -- node[above, font=\small] {$Q \cdot K^T$} (attn);
    \path [line] (attn) -- node[right, font=\small] {$\text{Attn} \cdot V$} (out_v);
    \path [line] (out_v) -- (reshape);
    \path [line] (reshape) -- (out);
\end{tikzpicture}
\caption{Chi tiết luồng xử lý và sự biến đổi kích thước tensor trong module Attention.}
\label{fig:attention_flow}
\end{figure}

\subsection{Khối PSABlock (Position-Sensitive Attention Block)}
\label{subsec:psablock}

PSABlock tích hợp cơ chế Self-Attention bên trên và một khối Feed-Forward Network (FFN) theo dạng nối tiếp, đi kèm hai kết nối tắt (residual connection):

\begin{figure}[H]
\centering
\begin{tikzpicture}[node distance=0.8cm and 1.2cm, auto]
    % Nodes
    \node (input) [splitnode] {};
    \node (lbl_in) [left=0.15cm of input] {Input $x$};
    \node (attn) [block, right=of input] {Attention};
    \node (sum1) [sum, right=of attn];
    \node (split1) [splitnode, right=of sum1, xshift=-0.3cm] {};
    \node (ffn) [orangeblock, right=of split1] {FFN};
    \node (sum2) [sum, right=of ffn];
    \node (output) [right=0.8cm of sum2] {Output};

    % Connections
    \path [line] (input) -- (attn);
    \path [line] (attn) -- (sum1);
    \path [line] (sum1) -- (split1);
    \path [line] (split1) -- (ffn);
    \path [line] (ffn) -- (sum2);
    \path [line] (sum2) -- (output);
    
    % Skip 1 (Attention residual)
    \path [line] (input) -- ++(0,1.0) -| (sum1.north);
    
    % Skip 2 (FFN residual)
    \path [line] (split1) -- ++(0,1.0) -| (sum2.north);
\end{tikzpicture}
\caption{Sơ đồ PSABlock: Attention + FFN với các kết nối residual.}
\label{fig:psablock}
\end{figure}

\begin{align}
    x_1 &= x + \text{Attention}(x) \quad \text{(Residual Attention)} \\
    x_2 &= x_1 + \text{FFN}(x_1) \quad \text{(Residual FFN)}
\end{align}

FFN (Feed-Forward Network) gồm lớp Conv $1\times1$ tăng số kênh ($c \to 2c$), đi qua hàm kích hoạt SiLU, và lớp Conv $1\times1$ nén kênh ngược về ban đầu ($2c \to c$, act=False):
\begin{equation}
    \text{FFN}(x_1) = \text{cv2}\Big(\text{SiLU}\big(\text{cv1}(x_1)\big)\Big)
\end{equation}

\begin{figure}[H]
\centering
\begin{tikzpicture}[node distance=1.0cm and 1.2cm, auto]
    % Nodes
    \node (input) [] {Input $x_1$ (Kênh $c$)};
    \node (cv1) [block, right=of input] {Conv 1x1 + SiLU\\(cv1: $c \to 2c$)};
    \node (cv2) [block, right=of cv1] {Conv 1x1\\(cv2: $2c \to c$)};
    \node (output) [right=of cv2] {Output (Kênh $c$)};

    % Connections
    \path [line] (input) -- (cv1);
    \path [line] (cv1) -- (cv2);
    \path [line] (cv2) -- (output);
\end{tikzpicture}
\caption{Sơ đồ khối FFN (Feed-Forward Network) trong PSABlock.}
\label{fig:ffn_block}
\end{figure}

\subsection{Khối C2PSA (Channel-Split Position-Sensitive Attention)}
\label{subsec:c2psa}

Khối C2PSA áp dụng cơ chế chia nhánh (Channel-Split) của CSPNet kết hợp với PSABlock nhằm tối ưu hóa chi phí tính toán của cơ chế Self-Attention:

\begin{figure}[H]
\centering
\begin{tikzpicture}[node distance=0.6cm and 0.8cm, auto]
    % Nodes
    \node (input) [] {Input $x$};
    \node (cv1) [block, right=of input] {Conv 1x1\\(cv1)};
    \node (split) [splitnode, right=of cv1, xshift=-0.2cm] {};
    \node (psablock) [orangeblock, right=of split] {PSABlock};
    \node (concat) [grayblock, right=of psablock] {Concat};
    \node (cv2) [block, right=of concat] {Conv 1x1\\(cv2)};
    \node (output) [right=of cv2] {Output};

    % Connections
    \path [line] (input) -- (cv1);
    \path [line] (cv1) -- (split);
    \path [line] (split) -- (psablock);
    \path [line] (psablock) -- (concat);
    \path [line] (concat) -- (cv2);
    \path [line] (cv2) -- (output);
    
    % Upper shortcut (Branch a)
    \coordinate (SplitUp) at ($(split)+(0,1.0)$);
    \path [line] (split) -- (SplitUp) -- node[above, midway, font=\small] {Nhánh $a$ (đi thẳng)} (SplitUp -| concat.north) -- (concat.north);
\end{tikzpicture}
\caption{Sơ đồ C2PSA: CSP splitting + PSABlock.}
\label{fig:c2psa}
\end{figure}

\begin{align}
    a, b &= \text{split}(\text{cv1}(x)) \\
    b' &= \text{PSABlock}(b) \\
    \text{Output} &= \text{cv2}(\text{Concat}[a, b'])
\end{align}

Đầu vào qua \texttt{cv1} được phân đôi: nhánh $a$ đi thẳng bảo toàn đặc trưng tích chập cục bộ, nhánh $b$ đi qua PSABlock học các phụ thuộc không gian xa. Cuối cùng, hai nhánh được concat và hợp nhất qua \texttt{cv2}.

\begin{figure}[H]
\centering
\begin{tikzpicture}[auto]
    % Input grid
    \draw[step=0.2cm, gray!20, very thin] (0,0) grid (2,2);
    \draw[thick, draw=black] (0,0) rectangle (2,2);
    \draw[fill=blue!40] (0.8, 0.8) rectangle (1.2, 1.2);
    \node[below=0.3cm, font=\small\bfseries, align=center] at (1, 0) {Đặc trưng cục bộ};
    
    % Arrow 1
    \draw[thick, ->] (2.4, 1) -- node[above=2pt, font=\small\bfseries] {SPPF} (3.9, 1);
    
    % SPPF output grid (shifted by 4.3cm)
    \begin{scope}[xshift=4.3cm]
        \draw[step=0.2cm, gray!20, very thin] (0,0) grid (2,2);
        \draw[thick, draw=black] (0,0) rectangle (2,2);
        \draw[fill=blue!40] (0.8, 0.8) rectangle (1.2, 1.2);
        % Concentric rings of receptive fields
        \draw[orange, thick, dashed] (1,1) circle (0.4cm);
        \draw[red, thick, dashed] (1,1) circle (0.8cm);
        \node[below=0.3cm, font=\small\bfseries, align=center] at (1, 0) {Đa quy mô\\(Multi-scale)};
    \end{scope}
    
    % Arrow 2
    \draw[thick, ->] (6.7, 1) -- node[above=2pt, font=\small\bfseries] {C2PSA} (8.2, 1);
    
    % C2PSA output grid (shifted by 8.6cm)
    \begin{scope}[xshift=8.6cm]
        \draw[step=0.2cm, gray!20, very thin] (0,0) grid (2,2);
        \draw[thick, draw=black] (0,0) rectangle (2,2);
        \draw[fill=blue!40] (0.8, 0.8) rectangle (1.2, 1.2);
        % Arrows pointing everywhere representing global attention
        \foreach \x in {0.2, 0.6, 1.4, 1.8} {
            \foreach \y in {0.2, 0.6, 1.4, 1.8} {
                \draw[blue!60, -latex, thin] (1,1) -- (\x, \y);
            }
        }
        \node[below=0.3cm, font=\small\bfseries, align=center] at (1, 0) {Ngữ cảnh toàn cục};
    \end{scope}
\end{tikzpicture}
\caption{Mô phỏng sự biến đổi của feature map khi đi qua SPPF và C2PSA.}
\label{fig:sppf_c2psa_transform}
\end{figure}

\begin{notebox}[Tại sao chỉ dùng Attention ở P5?]
Độ phức tạp tính toán của cơ chế Self-Attention là $O(N^2)$ với $N = H \times W$ là số điểm ảnh. Ở tầng P5 ($10 \times 10$), $N=100$ nên chi phí cực kỳ nhẹ. Ngược lại, ở tầng P3 ($40 \times 40$), $N=1600$ khiến độ phức tạp tăng gấp 256 lần, quá nặng cho các mô hình nhỏ như YOLO26-Nano.
\end{notebox}

\subsection{Tổng quan đồ thị các khối đặc biệt}
\label{subsec:blocks_overview}

Để trực quan hóa sự sắp đặt các khối tùy biến, đồ thị dưới đây biểu diễn mối liên kết giữa C3k2, SPPF và C2PSA trong mô hình YOLO26:

\begin{figure}[H]
\centering
\begin{tikzpicture}[node distance=0.8cm and 0.4cm, auto]
    % Nodes
    \node (p5_in) [grayblock] {P5 Backbone};
    \node (c3k2) [block, right=of p5_in] {C3k2\\ (c3k=True)};
    \node (sppf) [orangeblock, right=of c3k2] {SPPF};
    \node (c2psa) [purpleblock, right=of sppf] {C2PSA};
    \node (neck) [grayblock, right=of c2psa] {Neck / Head};

    % Connections
    \path [line] (p5_in) -- (c3k2);
    \path [line] (c3k2) -- (sppf);
    \path [line] (sppf) -- (c2psa);
    \path [line] (c2psa) -- (neck);
\end{tikzpicture}
\caption{Tổng quan cách tổ chức các khối đặc biệt: C3k2, SPPF, và C2PSA trong YOLO26.}
\label{fig:blocks_overview}
\end{figure}

\subsection{Bảng chi tiết các lớp Backbone}

\begin{table}[H]
\centering
\small
\caption{Chi tiết các lớp trong Backbone của YOLO26n (scale nano, ảnh $320\times320$).}
\label{tab:backbone_layers}
\begin{tabular}{clllll}
\toprule
\textbf{Layer} & \textbf{Module} & \textbf{Input} & \textbf{Output (n)} & \textbf{Key Args} & \textbf{Note} \\
\midrule
b0  & Conv   & $3\times320^2$ & $16\times160^2$ & $k\!=\!3, s\!=\!2$ & P1/2 \\
b1  & Conv   & $16\times160^2$ & $32\times80^2$ & $k\!=\!3, s\!=\!2$ & P2/4 \\
b2  & C3k2   & $32\times80^2$ & $64\times80^2$ & c3k=F, $e\!=\!0.25$ & \\
b3  & Conv   & $64\times80^2$ & $64\times40^2$ & $k\!=\!3, s\!=\!2$ & \textbf{P3/8} \\
b4  & C3k2   & $64\times40^2$ & $128\times40^2$ & c3k=F, $e\!=\!0.25$ & \\
b5  & Conv   & $128\times40^2$ & $128\times20^2$ & $k\!=\!3, s\!=\!2$ & \textbf{P4/16} \\
b6  & C3k2   & $128\times20^2$ & $128\times20^2$ & c3k=T, $e\!=\!0.5$ & \\
b7  & Conv   & $128\times20^2$ & $256\times10^2$ & $k\!=\!3, s\!=\!2$ & \textbf{P5/32} \\
b8  & C3k2   & $256\times10^2$ & $256\times10^2$ & c3k=T, $e\!=\!0.5$ & \\
b9  & SPPF   & $256\times10^2$ & $256\times10^2$ & $k\!=\!5, n\!=\!3$ & \\
b10 & C2PSA  & $256\times10^2$ & $256\times10^2$ & attention, $e\!=\!0.5$ & \\
\bottomrule
\end{tabular}
\end{table}

% ===================================================================
\section{Neck}
\label{sec:arch_neck}

Neck của \yolo sử dụng kiến trúc kết hợp FPN (top-down) và PAN (bottom-up) để tổng hợp đặc trưng đa tỷ lệ từ Backbone trước khi đưa vào Detection Head.

\subsection{Top-Down Pathway (FPN)}

Truyền semantic information từ tầng sâu (P5) xuống tầng nông (P3):

\begin{enumerate}
    \item Upsample b10 ($256 \times 10^2$) $\times 2 \to 256 \times 20^2$
    \item Concat với b6 ($128 \times 20^2$) $\to 384 \times 20^2$
    \item C3k2 $\to$ h13: $128 \times 20^2$
    \item Upsample h13 $\times 2 \to 128 \times 40^2$
    \item Concat với b4 ($128 \times 40^2$) $\to 256 \times 40^2$
    \item C3k2 $\to$ h16: $64 \times 40^2$ \quad (P3 output)
\end{enumerate}

\subsection{Bottom-Up Pathway (PAN)}

Truyền detail information từ tầng nông (P3) lên tầng sâu (P5):

\begin{enumerate}
    \item Conv stride-2: h16 ($64 \times 40^2$) $\to 64 \times 20^2$
    \item Concat với h13 ($128 \times 20^2$) $\to 192 \times 20^2$
    \item C3k2 $\to$ h19: $128 \times 20^2$ \quad (P4 output)
    \item Conv stride-2: h19 ($128 \times 20^2$) $\to 128 \times 10^2$
    \item Concat với b10 ($256 \times 10^2$) $\to 384 \times 10^2$
    \item C3k2(attn=True) $\to$ h22: $256 \times 10^2$ \quad (P5 output)
\end{enumerate}

Neck tạo ra 3 đầu ra ở 3 mức phân giải:
\begin{itemize}
    \item \textbf{P3\_out} (h16): $80 \times 80$ (với ảnh $640\times640$) hoặc $40 \times 40$ (với ảnh $320\times320$), stride 8 --- phát hiện vật thể \textbf{nhỏ}.
    \item \textbf{P4\_out} (h19): $40 \times 40$ (với ảnh $640\times640$) hoặc $20 \times 20$ (với ảnh $320\times320$), stride 16 --- phát hiện vật thể \textbf{trung bình}.
    \item \textbf{P5\_out} (h22): $20 \times 20$ (với ảnh $640\times640$) hoặc $10 \times 10$ (với ảnh $320\times320$), stride 32 --- phát hiện vật thể \textbf{lớn}.
\end{itemize}

\subsection{Đồ thị luồng dữ liệu FPN + PAN}

Dưới đây là sơ đồ chi tiết luồng xử lý và cách kết hợp các tầng đặc trưng trong Neck của YOLO26:

\begin{figure}[H]
\centering
\begin{tikzpicture}[
    node distance=0cm, auto,
    every node/.style={font=\small},
    block/.style={rectangle, draw=blue!70!black, fill=blue!5, thick, rounded corners, minimum width=2.4cm, minimum height=0.8cm, align=center},
    grayblock/.style={rectangle, draw=gray!70!black, fill=gray!10, thick, rounded corners, minimum width=2.4cm, minimum height=0.8cm, align=center},
    purpleblock/.style={rectangle, draw=purple!70!black, fill=purple!5, thick, rounded corners, minimum width=2.4cm, minimum height=0.8cm, align=center},
    orangeblock/.style={rectangle, draw=orange!70!black, fill=orange!5, thick, rounded corners, minimum width=2.4cm, minimum height=0.8cm, align=center},
    greenblock/.style={rectangle, draw=green!70!black, fill=green!5, thick, rounded corners, minimum width=2.4cm, minimum height=0.8cm, align=center},
    line/.style={draw, thick, ->, >=latex}
]
    % --- Left Column (Backbone) ---
    \node (b10) at (0, 0)      [grayblock] {\textbf{Backbone P5 (b10)}\\$256 \times 10^2$};
    \node (b6)  at (0, -3.0)   [grayblock] {\textbf{Backbone P4 (b6)}\\$128 \times 20^2$};
    \node (b4)  at (0, -7.8)   [grayblock] {\textbf{Backbone P3 (b4)}\\ $128 \times 40^2$};

    % --- Middle Column (FPN) ---
    \node (up1)  at (3.8, -1.5)  [purpleblock] {\textbf{Upsample 2x}\\$256 \times 20^2$};
    \node (cat1) at (3.8, -3.0)  [grayblock] {\textbf{Concat}};
    \node (h13)  at (3.8, -4.8)  [block] {\textbf{C3k2 (h13)}\\$128 \times 20^2$};
    \node (up2)  at (3.8, -6.3)  [purpleblock] {\textbf{Upsample 2x}\\$128 \times 40^2$};
    \node (cat2) at (3.8, -7.8)  [grayblock] {\textbf{Concat}};
    \node (h16)  at (3.8, -9.3)  [block] {\textbf{C3k2 (h16)}\\$64 \times 40^2$};
    \node (p3_out) at (3.8, -10.8) [greenblock] {\textbf{P3\_out (to Head)}\\$64 \times 40^2$};

    % --- Right Column (PAN) ---
    \node (h17)  at (7.6, -6.3)  [orangeblock] {\textbf{Conv (s=2)}\\$64 \times 20^2$};
    \node (cat3) at (7.6, -4.8)  [grayblock] {\textbf{Concat}};
    \node (h19)  at (7.6, -3.0)  [block] {\textbf{C3k2 (h19)}\\$128 \times 20^2$};
    \node (h20)  at (7.6, -1.5)  [orangeblock] {\textbf{Conv (s=2)}\\ $128 \times 10^2$};
    \node (cat4) at (7.6, 0)     [grayblock] {\textbf{Concat}};
    \node (h22)  at (7.6, 1.5)   [block] {\textbf{C3k2+Attn (h22)}\\$256 \times 10^2$};

    % --- Detection Outputs ---
    \node (p4_out) at (11.2, -3.0) [greenblock] {\textbf{P4\_out (to Head)}\\$128 \times 20^2$};
    \node (p5_out) at (11.2, 1.5)  [greenblock] {\textbf{P5\_out (to Head)}\\$256 \times 10^2$};

    % --- Connections ---
    % b10 splits to cat4 (horizontal) and up1 (orthogonal)
    \coordinate (b10_split) at (3.8, 0);
    \draw [thick] (b10.east) -- (b10_split);
    \path [line] (b10_split) -- (cat4.west);
    \path [line] (b10_split) -- (up1.north);

    % FPN column
    \path [line] (up1.south) -- (cat1.north);
    \path [line] (b6.east) -- (cat1.west);
    \path [line] (cat1.south) -- (h13.north);
    \path [line] (h13.south) -- (up2.north);
    \path [line] (up2.south) -- (cat2.north);
    \path [line] (b4.east) -- (cat2.west);
    \path [line] (cat2.south) -- (h16.north);
    \path [line] (h16.south) -- (p3_out.north);

    % PAN column
    \path [line] (h16.east) -| (h17.south);
    \path [line] (h17.north) -- (cat3.south);
    \path [line] (h13.east) -- (cat3.west);
    \path [line] (cat3.north) -- (h19.south);
    \path [line] (h19.north) -- (h20.south);
    \path [line] (h20.north) -- (cat4.south);
    \path [line] (cat4.north) -- (h22.south);

    % Outputs
    \path [line] (h19.east) -- (p4_out.west);
    \path [line] (h22.east) -- (p5_out.west);
\end{tikzpicture}
\caption{Kiến trúc tổng hợp đặc trưng Neck (FPN + PAN) của YOLO26n với kích thước đặc trưng chi tiết cho ảnh đầu vào $320\times320$.}
\label{fig:yolo26_neck_flow}
\end{figure}

\subsection{Bảng chi tiết các lớp Neck}

\begin{table}[H]
\centering
\small
\caption{Chi tiết các lớp trong Neck của YOLO26n (scale nano, ảnh $320\times320$).}
\label{tab:neck_layers}
\begin{tabular}{clllll}
\toprule
\textbf{Layer} & \textbf{Module} & \textbf{Input} & \textbf{Output (n)} & \textbf{Key Args} & \textbf{Note} \\
\midrule
h11 & Upsample & b10 ($256\times10^2$) & $256\times20^2$ & scale=2, nearest & Upsample P5 \\
h12 & Concat   & h11, b6               & $384\times20^2$ & dim=1            & Concat với Backbone P4 \\
h13 & C3k2     & h12                   & $128\times20^2$ & c3k=T, $e\!=\!0.5$ & Trích xuất đặc trưng P4 \\
h14 & Upsample & h13 ($128\times20^2$) & $128\times40^2$ & scale=2, nearest & Upsample P4 \\
h15 & Concat   & h14, b4               & $256\times40^2$ & dim=1            & Concat với Backbone P3 \\
h16 & C3k2     & h15                   & $64\times40^2$  & c3k=T, $e\!=\!0.5$ & \textbf{P3 Output (Vật nhỏ)} \\
h17 & Conv     & h16 ($64\times40^2$)  & $64\times20^2$  & $k\!=\!3, s\!=\!2$ & Giảm tỷ lệ P3 xuống P4 \\
h18 & Concat   & h17, h13              & $192\times20^2$ & dim=1            & Concat với FPN P4 \\
h19 & C3k2     & h18                   & $128\times20^2$ & c3k=T, $e\!=\!0.5$ & \textbf{P4 Output (Vật vừa)} \\
h20 & Conv     & h19 ($128\times20^2$) & $128\times10^2$ & $k\!=\!3, s\!=\!2$ & Giảm tỷ lệ P4 xuống P5 \\
h21 & Concat   & h20, b10              & $384\times10^2$ & dim=1            & Concat với Backbone P5 \\
h22 & C3k2     & h21                   & $256\times10^2$ & c3k=T, attn=T    & \textbf{P5 Output (Vật lớn)} \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[H]
\centering
\caption{Số anchor points tại mỗi scale detection (ảnh $320\times320$).}
\label{tab:anchors}
\begin{tabular}{lcccc}
\toprule
\textbf{Scale} & \textbf{Stride} & \textbf{Feature Map} & \textbf{Anchor Points} & \textbf{Phù hợp} \\
\midrule
P3 & 8  & $40\times40$  & 1600 & Vật nhỏ \\
P4 & 16 & $20\times20$  & 400  & Vật vừa \\
P5 & 32 & $10\times10$  & 100  & Vật lớn \\
\midrule
\multicolumn{3}{r}{\textbf{Tổng}} & \textbf{2100} & \\
\bottomrule
\end{tabular}
\end{table}

% ===================================================================
\section{Detection Head: Dual-Head O2M/O2O}
\label{sec:arch_head}

\yolo sử dụng kiến trúc Dual-Head với hai nhánh dự đoán song song:

\subsection{Nhánh cv2 --- Box Prediction}

Dự đoán tọa độ bounding box thông qua 2 lớp Conv $3 \times 3$ và một lớp Conv $1 \times 1$ cuối:

\begin{lstlisting}[caption={Nhánh cv2 (Box) trong Detection Head}, label=lst:cv2]
self.cv2 = nn.ModuleList(
    nn.Sequential(
        Conv(x, c2, 3),                     # Conv 3x3
        Conv(c2, c2, 3),                    # Conv 3x3
        nn.Conv2d(c2, 4 * self.reg_max, 1)  # Conv 1x1 -> 4*reg_max
    ) for x in ch
)
\end{lstlisting}

Đầu ra: $4 \times \text{reg\_max}$ kênh. Với Direct Regression ($\text{reg\_max} = 1$), đầu ra chỉ có 4 kênh tương ứng 4 khoảng cách LTRB (left, top, right, bottom).

\subsection{Nhánh cv3 --- Class Prediction}

Dự đoán xác suất lớp đối tượng sử dụng Depthwise Conv để tối ưu tốc độ:

\begin{lstlisting}[caption={Nhánh cv3 (Class) trong Detection Head}, label=lst:cv3]
self.cv3 = nn.ModuleList(
    nn.Sequential(
        nn.Sequential(DWConv(x, x, 3), Conv(x, c3, 1)),  # DWConv + Conv
        nn.Sequential(DWConv(c3, c3, 3), Conv(c3, c3, 1)),
        nn.Conv2d(c3, self.nc, 1)  # Conv 1x1 -> nc classes
    ) for x in ch
)
\end{lstlisting}

\subsection{Cơ chế Dual-Head}

\begin{table}[H]
    \centering
    \caption{So sánh nhánh O2M và O2O trong Dual-Head}
    \label{tab:dual_head}
    \begin{tabularx}{\textwidth}{lXX}
        \toprule
        \textbf{Đặc tính} & \textbf{O2M (One-to-Many)} & \textbf{O2O (One-to-One)} \\
        \midrule
        Khi nào dùng & Chỉ khi \textbf{huấn luyện} & Khi \textbf{suy luận} (và cả huấn luyện) \\
        TAL topk     & $\text{topk} = 10$ & $\text{topk}_2 = 1$ \\
        Số dự đoán / vật thể & Nhiều anchor $\to$ 1 vật thể & Đúng 1 anchor $\to$ 1 vật thể \\
        Mục đích     & Tạo gradient dày đặc, đẩy nhanh tốc độ học & NMS-free, dự đoán sạch \\
        Cần NMS?     & Có (khi suy luận đơn lẻ) & \textbf{Không} \\
        \bottomrule
    \end{tabularx}
\end{table}

Sơ đồ kiến trúc nhánh kép (Dual-Head) được biểu diễn trực quan ở hình dưới:

\begin{figure}[H]
\centering
\begin{tikzpicture}[
    node distance=0cm, auto,
    every node/.style={font=\small},
    block/.style={rectangle, draw=blue!70!black, fill=blue!5, thick, rounded corners, minimum width=2.4cm, minimum height=0.8cm, align=center},
    grayblock/.style={rectangle, draw=gray!70!black, fill=gray!10, thick, rounded corners, minimum width=2.4cm, minimum height=0.8cm, align=center},
    purpleblock/.style={rectangle, draw=purple!70!black, fill=purple!5, thick, rounded corners, minimum width=2.4cm, minimum height=0.8cm, align=center},
    orangeblock/.style={rectangle, draw=orange!70!black, fill=orange!5, thick, rounded corners, minimum width=2.4cm, minimum height=0.8cm, align=center},
    greenblock/.style={rectangle, draw=green!70!black, fill=green!5, thick, rounded corners, minimum width=2.6cm, minimum height=0.8cm, align=center},
    line/.style={draw, thick, ->, >=latex},
    dashedline/.style={draw, thick, dashed, ->, >=latex}
]
    % Nodes
    \node (neck) at (0, 0) [grayblock] {\textbf{Đặc trưng từ Neck}\\ (P3, P4, P5)};
    
    % O2M Branch
    \node (o2m_head) at (3.2, 1.2) [block] {\textbf{Nhánh O2M Head}\\ (cv2, cv3)};
    \node (o2m_loss) at (6.2, 1.2) [orangeblock] {\textbf{O2M Loss}\\ (TAL topk=10)};
    
    % O2O Branch
    \node (detach)   at (2.4, -1.2) [purpleblock] {\textbf{Detach()}\\ (Ngắt gradient)};
    \node (o2o_head) at (5.4, -1.2) [block] {\textbf{Nhánh O2O Head}\\ (one2one\_cv2, one2one\_cv3)};
    \node (o2o_loss) at (8.4, -1.2) [orangeblock] {\textbf{O2O Loss}\\ (TAL topk=1)};
    
    % Total Loss
    \node (total_loss) at (6.0, -2.6) [greenblock] {\textbf{Tổng Loss}\\ $\mathcal{L}_{\text{total}} = \mathcal{L}_{\text{o2m}} + \mathcal{L}_{\text{o2o}}$};

    % Connections
    \path [line] (neck.east) -- ++(0.4,0) |- (o2m_head.west);
    \path [line] (neck.east) -- ++(0.4,0) |- (detach.west);
    \path [line] (detach.east) -- (o2o_head.west);
    \path [line] (o2m_head.east) -- (o2m_loss.west);
    \path [line] (o2o_head.east) -- (o2o_loss.west);
    
    % Routing lines from loss nodes to total_loss around the right side without overlaps
    \path [line] (o2m_loss.east) -- (10.0, 1.2) |- (total_loss.east);
    \path [line] (o2o_loss.east) -- (10.0, -1.2) |- (total_loss.east);
    
    % Gradient flow
    \path [dashedline] (o2m_loss.north) -- ++(0,0.5) -| node[above, pos=0.25, font=\scriptsize] {Lan truyền ngược (Gradient)} (neck.north);
\end{tikzpicture}
\caption{Sơ đồ cấu trúc Dual-Head (O2M/O2O) và cơ chế ngắt lan truyền ngược gradient của nhánh O2O để bảo vệ các đặc trưng chung ở Backbone và Neck.}
\label{fig:dual_head_flow}
\end{figure}

Khi deploy thực tế, nhánh O2M bị loại bỏ hoàn toàn thông qua phương thức \texttt{Detect.fuse()} (gán \texttt{cv2 = cv3 = None}), chỉ giữ lại nhánh O2O phục vụ suy luận trực tiếp.

\begin{table}[H]
\centering
\caption{So sánh model trước và sau khi strip O2M.}
\label{tab:strip_o2m}
\begin{tabular}{lccc}
\toprule
\textbf{Metric} & \textbf{Training (last.pt)} & \textbf{Inference (lite.pt)} & \textbf{Giảm} \\
\midrule
File size & 38.2 MB & 5.6 MB & $\sim$85\% \\
Precision & FP32 & FP16 & 50\% \\
O2M Head & Có & Stripped & -- \\
Optimizer state & Có & Stripped & -- \\
\bottomrule
\end{tabular}
\end{table}

% ===================================================================
\section{Box Decoding: Direct Regression}
\label{sec:arch_box_decoding}

\yolo sử dụng phương pháp hồi quy trực tiếp (Direct Regression) thay cho DFL~\cite{dfl}, giúp giảm đáng kể khối lượng tính toán:

\begin{enumerate}[label=\textbf{Bước \arabic*:}]
    \item \textbf{Feature Extraction}: Trích xuất đặc trưng qua Backbone và Neck.
    \item \textbf{Tạo Anchor Grids}: Xác định tọa độ tâm $(c_x, c_y)$ cho mỗi ô lưới.
    \item \textbf{Dự đoán LTRB}: Nhánh cv2 xuất ra 4 giá trị khoảng cách: $d_\text{left}$, $d_\text{top}$, $d_\text{right}$, $d_\text{bottom}$.
    \item \textbf{Khôi phục tọa độ}:
    \begin{equation}
        x_\text{min} = c_x - d_\text{left}, \quad y_\text{min} = c_y - d_\text{top}
    \end{equation}
    \begin{equation}
        x_\text{max} = c_x + d_\text{right}, \quad y_\text{max} = c_y + d_\text{bottom}
    \end{equation}
    \item \textbf{Rescale}: Nhân tọa độ với stride tương ứng để đưa về kích thước ảnh gốc.
\end{enumerate}

\begin{figure}[H]
\centering
\begin{tikzpicture}[scale=1.2]
    % Draw grid
    \draw[step=1cm, gray!40, thick] (-2,-2) grid (2,2);
    
    % Draw anchor point (cx, cy)
    \filldraw[blue] (0.5,0.5) circle (2pt) node[anchor=north east, font=\small] {$(c_x, c_y)$};
    
    % Draw Bounding Box (xmin, ymin) to (xmax, ymax)
    \draw[red, ultra thick] (-1.2,-0.8) rectangle (1.5,1.8);
    \node[red, anchor=south west, font=\small] at (1.5,1.8) {$(x_{\text{max}}, y_{\text{max}})$};
    \node[red, anchor=north east, font=\small] at (-1.2,-0.8) {$(x_{\text{min}}, y_{\text{min}})$};
    
    % Draw arrows for distances
    \draw[latex-latex, blue, thick] (-1.2,0.5) -- (0.5,0.5) node[midway, above, font=\scriptsize] {$d_{\text{left}}$};
    \draw[latex-latex, blue, thick] (0.5,0.5) -- (1.5,0.5) node[midway, above, font=\scriptsize] {$d_{\text{right}}$};
    \draw[latex-latex, blue, thick] (0.5,0.5) -- (0.5,1.8) node[midway, right, font=\scriptsize] {$d_{\text{top}}$};
    \draw[latex-latex, blue, thick] (0.5,-0.8) -- (0.5,0.5) node[midway, right, font=\scriptsize] {$d_{\text{bottom}}$};
    
    % Draw dashed lines to edges for clarity
    \draw[dashed, gray] (-1.2,0.5) -- (-1.2,-0.8);
    \draw[dashed, gray] (1.5,0.5) -- (1.5,1.8);
    \draw[dashed, gray] (0.5,1.8) -- (1.5,1.8);
    \draw[dashed, gray] (0.5,-0.8) -- (-1.2,-0.8);
\end{tikzpicture}
\caption{Cơ chế giải mã tọa độ bounding box theo khoảng cách LTRB từ tâm ô lưới $(c_x, c_y)$.}
\label{fig:ltrb_decoding}
\end{figure}

\subsection{Cơ chế DFL (Distribution Focal Loss)}
\label{subsec:dfl_module}

DFL~\cite{dfl} chuyển bài toán hồi quy khoảng cách liên tục thành bài toán phân loại trên một số lượng $\texttt{reg\_max}$ các bins rời rạc:

\begin{equation}
    \hat{d} = \sum_{i=0}^{\texttt{reg\_max}-1} i \cdot p_i, \quad p_i = \text{softmax}(\text{logits})_i
\end{equation}

Trọng số của lớp Conv $1\times1$ cuối được cố định là chuỗi tăng dần $[0, 1, 2, \ldots, \texttt{reg\_max}-1]$ và không tham gia vào quá trình lan truyền ngược để cập nhật tham số.

\begin{notebox}[YOLO26 mặc định: reg\_max=1 --- không dùng DFL]
Khi đặt cấu hình mặc định \texttt{reg\_max=1}, DFL sẽ tự động được thay thế bằng một lớp đồng nhất \texttt{nn.Identity()}. Lúc này mô hình thực hiện hồi quy trực tiếp 4 giá trị khoảng cách $(l, t, r, b)$, đầu ra của box head chỉ gồm 4 kênh. Với cấu hình \texttt{reg\_max=16}, đầu ra sẽ có 64 kênh, cho phép dự đoán phân phối xác suất biên tốt hơn nhưng tăng chi phí tính toán.
\end{notebox}

\subsection{So sánh Direct Regression và DFL}

\begin{table}[H]
    \centering
    \caption{So sánh hồi quy trực tiếp (Direct Regression) và DFL}
    \label{tab:direct_vs_dfl}
    \begin{tabularx}{\textwidth}{lXX}
        \toprule
        \textbf{Đặc tính} & \textbf{Direct Regression} & \textbf{DFL} \\
        \midrule
        Cách tính & Xuất trực tiếp 4 giá trị $l, r, t, b$ & Softmax 16 giá trị/hướng, tổng trọng số \\
        Kênh đầu ra & $4 \times 1 = 4$ & $4 \times 16 = 64$ \\
        Ưu điểm & Tốc độ nhanh, đơn giản & Chính xác hơn ở biên mờ \\
        Nhược điểm & Kém chính xác ở biên mờ & Softmax tốn CPU, gây nghẽn \\
        \bottomrule
    \end{tabularx}
\end{table}

\yolo bù đắp sự suy giảm độ chính xác của Direct Regression bằng cách đặt $\text{topk}=10$ ở nhánh O2M, tạo gradient dày đặc ép Backbone và Neck học đặc trưng sắc nét hơn.

% ===================================================================
\section{Phân tỷ lệ mô hình (Model Scaling)}
\label{sec:arch_scaling}

\yolo hỗ trợ nhiều biến thể kích thước thông qua 3 hệ số:

\begin{table}[H]
\centering
\caption{Cấu hình Scale của YOLO26.}
\label{tab:scales}
\begin{tabular}{lcccl}
\toprule
\textbf{Scale} & \textbf{Depth} & \textbf{Width} & \textbf{Max Ch} & \textbf{Ghi chú} \\
\midrule
n (nano)   & 0.50 & 0.25 & 1024 & Nhỏ nhất, nhanh nhất \\
s (small)  & 0.50 & 0.50 & 1024 & Cân bằng tốc độ/chính xác \\
m (medium) & 0.50 & 1.00 & 512  & Trung bình \\
l (large)  & 1.00 & 1.00 & 512  & Lớn \\
x (xlarge) & 1.00 & 1.50 & 512  & Chính xác nhất \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Công thức tính kênh và lặp:}
\begin{align}
    \texttt{ch}(c) &= \texttt{make\_divisible}\left(\min(c, \texttt{max\_ch}) \times \texttt{width},\; 8\right) \label{eq:ch} \\
    \texttt{rep}(n) &= \max\left(\text{round}(n \times \texttt{depth}),\; 1\right) \label{eq:rep}
\end{align}

Ví dụ scale nano: $\texttt{ch}(256) = \texttt{make\_divisible}(\min(256, 1024) \times 0.25, 8) = 64$.

\subsection{Bảng so sánh kích thước các tầng đặc trưng qua các Scale}

Dưới đây là bảng so sánh số kênh đầu ra tại các lớp Backbone và Neck quan trọng trên cả 5 cấu hình scale:

\begin{table}[H]
\centering
\small
\caption{Số kênh của các lớp đặc trưng quan trọng trong YOLO26 trên các scale khác nhau.}
\label{tab:scale_channels}
\begin{tabular}{lcccccc}
\toprule
\textbf{Tầng} & \textbf{Kênh Cơ sở} & \textbf{Nano (n)} & \textbf{Small (s)} & \textbf{Medium (m)} & \textbf{Large (l)} & \textbf{X-Large (x)} \\
\midrule
b0 (Conv 1)   & 64                  & 16                & 32                 & 64                  & 64                 & 96                   \\
b1 (Conv 2)   & 128                 & 32                & 64                 & 128                 & 128                & 192                  \\
b2 (C3k2)     & 256                 & 64                & 128                & 256                 & 256                & 384                  \\
b4 (C3k2)     & 512                 & 128               & 256                & 512                 & 512                & 768                  \\
b6 (C3k2)     & 512                 & 128               & 256                & 512                 & 512                & 768                  \\
b8 (C3k2)     & 1024                & 256               & 512                & 512                 & 512                & 768                  \\
b10 (C2PSA)   & 1024                & 256               & 512                & 512                 & 512                & 768                  \\
\midrule
h13 (C3k2)    & 512                 & 128               & 256                & 512                 & 512                & 768                  \\
h16 (P3 Output)& 256                & 64                & 128                & 256                 & 256                & 384                  \\
h19 (P4 Output)& 512                & 128               & 256                & 512                 & 512                & 768                  \\
h22 (P5 Output)& 1024               & 256               & 512                & 512                 & 512                & 768                  \\
\midrule
\textbf{Số lần lặp block} & & & & & & \\
$\texttt{rep}(2)$ & 2               & 1                 & 1                  & 1                   & 2                  & 2                    \\
\bottomrule
\end{tabular}
\end{table}

Trong đồ án, nhóm sử dụng biến thể \textbf{nano} (n) với tổng dung lượng weights khoảng 5.3MB, phù hợp cho triển khai trên \rpi.
